import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The default bottom-left badge covers the first item in the mobile bottom navigation.
  devIndicators: false,
};

export default nextConfig;
